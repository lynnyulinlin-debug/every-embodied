# OpenClaw 适配层

这个目录用于放置公开教程需要的 OpenClaw 调用适配代码。

当前公开版本只保留接口说明，不包含真实机械臂控制、场景编辑器或本机调试脚本。后续接入真实 OpenClaw 时，建议把上层业务逻辑和设备动作隔离：

- `inventory.py` 负责库存状态；
- `workflow_hooks.py` 负责把库存状态转换为任务；
- `openclaw_like/` 负责把任务翻译成 OpenClaw 可执行动作。

这样读者可以先复现家庭物资助手的数据闭环，再按自己的设备环境接入真实控制接口。
